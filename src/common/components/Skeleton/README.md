### Skeleton

```js
import { Skeleton } from "@src/common/components/Skeleton";

<Skeleton variant={"rect"} width={} height={} />;
```

```js
Skeleton:
    variant  指定形状	 'rect' | 'circle'
    width    宽度
    height   高度
```
